# .git stays in the context when present: the identity stage clones it to derive
# the release. Render omits .git; the stage then commits the context itself.
.page2webmcp
.env*
!.env.example
node_modules
**/node_modules
**/.next
.dist/*
!.dist/deployment-identity.json
!.dist/deployment-source.tar
playwright-report
test-results
.superpowers
docs/superpowers/.DS_Store
