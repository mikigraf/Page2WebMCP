const capabilities = [["find_record", "R0", "Proposed"], ["get_record_status", "R0", "Proposed"], ["create_request", "R1", "Requires confirmation"], ["delete_account", "R3", "Blocked"]];
export default function DemoProjectPage() { return <main><h1>Example capability review</h1><p>Runtime, OpenAPI, and source evidence are reviewed before an immutable release can be published.</p><table><thead><tr><th>Capability</th><th>Risk</th><th>Status</th></tr></thead><tbody>{capabilities.map(([name, risk, status]) => <tr key={name}><td><code>{name}</code></td><td>{risk}</td><td>{status}</td></tr>)}</tbody></table></main>; }
